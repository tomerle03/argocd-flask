apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/instance: test
  ports:
    - nodePort: 5000
      protocol: TCP
      port: 80
      targetPort: 5000
